---
description: Show current state (active case, auto_sync, sync_conflict, protocol). Soul is context-only — not shown.
allowed-tools:
  - Bash
---

Run `bibi-ctrl status` and return the output verbatim.
